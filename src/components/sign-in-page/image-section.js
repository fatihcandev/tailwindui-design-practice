import React from 'react'
import styles from '../../styles/sign-in-page/image-section.module.css'

const ImageSection = ({ children }) => (
  <div className={styles.imageSection}>{children}</div>
)

export default ImageSection
